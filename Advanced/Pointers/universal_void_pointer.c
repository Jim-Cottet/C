#include <stdio.h>

int main()
{
    int *ptr; // -> Pointer to an int
    // The dereferenced pointer will be treated as an integer

    double *dPtr; // -> Pointer to double

    char *chPtr; // Pointer to a char...

    void *vPtr; // Pointer to any type of Data :()
    /* Why is iy useful?
        1) For "General purpose" functions -> a function that will support different entry types may need an undetermined pointer
        2) Unknown returned pointer type
    */ 
    

    return 0;
}